node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
supabase/.temp/
backups/

# Secrets de la passerelle WhatsApp : clé API, clé Tailscale, mot de passe
# Postgres. Ils n'entrent jamais dans Git — prévoir leur transport à la main
# lors d'un déménagement de poste (voir evolution/README.md).
evolution/.env
!evolution/.env.example
